import { MaterialDirective } from './material.directive';

describe('MaterialDirective', () => {
  it('should create an instance', () => {
    const directive = new MaterialDirective();
    expect(directive).toBeTruthy();
  });
});
