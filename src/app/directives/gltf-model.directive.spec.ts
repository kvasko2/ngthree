import { GltfModelDirective } from './gltf-model.directive';

describe('GltfModelDirective', () => {
  it('should create an instance', () => {
    const directive = new GltfModelDirective();
    expect(directive).toBeTruthy();
  });
});
